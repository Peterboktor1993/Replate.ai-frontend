export const metadata = {
  title: "Your Order History | Cravio",
  description: "View and track all your food orders in one place.",
};

export default function OrdersLayout({ children }) {
  return <>{children}</>;
}
